package medicalApp.velociraptors.com;

import java.awt.Color;
import java.awt.Dimension;

import javax.swing.JLabel;
import javax.swing.JPanel;

@SuppressWarnings("serial")
public class LoginSuccessful extends JPanel {

	/**
	 * Create the panel.
	 */
	public LoginSuccessful(final MedAppController mac) {
		setBackground(Color.LIGHT_GRAY);
		setPreferredSize(new Dimension(800,600));
		setLayout(null);
		
		JLabel lblLoginSuccessful = new JLabel("Login Successful");
		lblLoginSuccessful.setBounds(262, 275, 359, 119);
		add(lblLoginSuccessful);

	}
}
